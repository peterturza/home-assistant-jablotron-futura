services:
  homeassistant:
    container_name: ha-jablotron-dev
    image: homeassistant/home-assistant:2025.1
    volumes:
      - ./dev-config:/config
      - ./custom_components:/config/custom_components
    ports:
      - "8123:8123"
    restart: unless-stopped
